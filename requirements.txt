# Lazy Lizard Agent — Python dependencies
# Pin exact versions for reproducibility and security.
# Run: pip install -r requirements.txt

# Async HTTP client
httpx==0.27.2

# Environment variable loading
python-dotenv==1.0.1

# Solana / Solders (optional — only needed for real transaction signing)
# Uncomment these when running outside dry-run mode:
# solana==0.34.3
# solders==0.21.0

# Telegram (alternative sync helper — we use httpx directly in bot.py)
# python-telegram-bot==21.6

# Testing
pytest==8.3.2
pytest-asyncio==0.23.8
